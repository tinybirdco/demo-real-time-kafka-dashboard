TOKEN "all_endpoints_read" READ

TOKEN "api_latency_endpoint_read_8407" READ

NODE endpoint
SQL >

    SELECT
      pipe_id,
      argMax(duration, start_datetime) AS latency,
      argMax(read_bytes, start_datetime) AS processed_bytes
    FROM tinybird.pipe_stats_rt
    GROUP BY pipe_id


