TOKEN "top_pages_endpoint_read_3207" READ

TOKEN "all_endpoints_read" READ

NODE endpoint
SQL >

    SELECT
      JSONExtractString(payload, 'pathname') AS page,
      count() AS c
    FROM events
    GROUP BY page
    ORDER BY c DESC
    LIMIT 5


